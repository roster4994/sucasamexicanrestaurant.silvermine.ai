---
import Layout from '../layouts/Layout.astro';

const deliveryPartners = [
  {
    name: "DoorDash",
    link: "https://www.doordash.com/store/su-casa-mexican-restaurant-midvale-922561/",
    color: "#d1615d"
  },
  {
    name: "GrubHub",
    link: "https://www.grubhub.com/restaurant/su-casa-mexican-restaurant-6878-state-st-midvale/1472764?orderMethod=delivery&locationMode=DELIVERY&facetSet=umamiV2&pageSize=20&hideHateos=true&searchMetrics=true&queryText=Su%20Casa%20Mexican%20Restaurant&latitude=40.58205795&longitude=-111.86144257&preciseLocation=true&facet=open_now%3Atrue&sortSetId=umamiv3&sponsoredSize=3&countOmittingTimes=true&whenFor=2020-03-19T17%3A30%3A00.000Z&isFutureOrder=true",
    color: "#66596e"
  },
  {
    name: "Uber Eats",
    link: "https://www.ubereats.com/store/su-casa-mexican-restaurant/x48U_gbMSeislBSIokiUXQ",
    color: "#74bab1"
  }
];
---

<Layout
  title="Order Online"
  description="Order Su Casa Mexican Restaurant for delivery or pickup through DoorDash, GrubHub, or Uber Eats. Authentic Mexican food delivered to your door in Midvale, UT."
>
<div class="order-page-wrapper">
  <!-- Banner -->
  <section class="page-banner">
    <img src="https://i.postimg.cc/D0bJtJwf/lobby4.jpg" alt="Su Casa Mexican Restaurant" class="banner-image" />
  </section>

  <div class="order-content">
    <div class="container">
      <!-- Main Order Button -->
      <section class="main-order-section">
        <a href="https://order.spoton.com/so-su-casa-mexican-restaurant-midvale-3200/midvale-ut/600f52b924b4c47e9cc94655" target="_blank" rel="noopener noreferrer" class="main-order-btn">
          DELIVERY & PICK-UP
        </a>
        <p class="order-note">Ordering directly through us supports our small business and our ability to serve you well, with third-party delivery options available below for your convenience.</p>
      </section>

      <!-- Delivery Partners -->
      <section class="delivery-section">
        <div class="partners-grid">
          {deliveryPartners.map((partner) => (
            <a href={partner.link} target="_blank" rel="noopener noreferrer" class="partner-card" style={`--partner-color: ${partner.color}`}>
              <h3>{partner.name}</h3>
            </a>
          ))}
        </div>
      </section>

      <!-- Pickup Option -->
      <section class="pickup-section">
        <div class="pickup-content">
          <div class="pickup-text">
            <h2>Prefer to Call?</h2>
            <a href="tel:+18012551042" class="btn btn-primary">(801) 255 - 1042</a>
          </div>
          <div class="pickup-info">
            <h3>Pickup Location</h3>
            <address>
              6878 State Street<br>
              Midvale, UT 84047
            </address>
            <p class="pickup-hours">
              <strong>Hours:</strong><br>
              Mon-Thu: 11am - 8pm<br>
              Fri-Sat: 11am - 9pm<br>
              Sunday: Closed
            </p>
          </div>
        </div>
      </section>

      <!-- Catering CTA -->
      <section class="catering-cta">
        <div class="cta-content">
          <div>
            <h2>Feeding a Group?</h2>
            <p>For large orders or catering needs, contact us directly for special pricing and menu options.</p>
          </div>
          <a href="/catering" class="btn btn-primary">Catering Info</a>
        </div>
      </section>
    </div>
  </div>
</div>
</Layout>

<style>
  /* Banner */
  .page-banner {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 350px;
    overflow: hidden;
    z-index: -1;
  }

  .order-page-wrapper {
    padding-top: 350px;
  }

  .banner-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .order-content {
    padding: var(--spacing-xl) 0;
  }

  /* Main Order Button */
  .main-order-section {
    text-align: center;
    margin-bottom: var(--spacing-xl);
  }

  .order-note {
    max-width: 600px;
    margin: var(--spacing-md) auto 0;
    color: var(--color-text-light);
    font-size: 0.95rem;
    line-height: 1.6;
  }

  .main-order-btn {
    display: inline-block;
    padding: 1.5rem 4rem;
    background: var(--color-primary);
    color: white;
    font-size: 1.8rem;
    font-weight: 700;
    text-decoration: none;
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-lg);
    transition: all var(--transition-normal);
    letter-spacing: 0.05em;
    border: 3px solid var(--color-tertiary);
  }

  .main-order-btn:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.25);
    background: var(--color-primary-dark);
  }

  /* Delivery Partners */
  .delivery-section {
    margin-bottom: var(--spacing-xl);
  }

  .partners-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: var(--spacing-md);
  }

  .partner-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
    padding: var(--spacing-md);
    background: var(--partner-color);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
    text-decoration: none;
    transition: all var(--transition-normal);
  }

  .partner-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-lg);
    filter: brightness(1.1);
  }

  .partner-card h3 {
    color: white;
    margin: 0;
    font-size: 1.3rem;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
  }

  /* Pickup Section */
  .pickup-section {
    background: var(--color-teal);
    padding: var(--spacing-lg);
    border-radius: var(--radius-lg);
    margin-bottom: var(--spacing-xl);
  }

  .pickup-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-lg);
    align-items: center;
  }

  .pickup-text h2 {
    color: white;
    margin-bottom: var(--spacing-sm);
  }

  .pickup-text .btn-primary {
    background: white;
    color: var(--color-teal);
    border-color: white;
  }

  .pickup-text .btn-primary:hover {
    background: var(--color-tertiary);
    border-color: var(--color-tertiary);
    color: var(--color-brown-dark);
  }

  .pickup-info {
    background: white;
    padding: var(--spacing-md);
    border-radius: var(--radius-md);
    box-shadow: var(--shadow-sm);
    border-left: 4px solid var(--color-tertiary);
  }

  .pickup-info h3 {
    color: var(--color-brown-dark);
    margin: 0 0 var(--spacing-xs);
    font-size: 1.1rem;
  }

  .pickup-info address {
    font-style: normal;
    color: var(--color-text);
    margin-bottom: var(--spacing-sm);
  }

  .pickup-hours {
    color: var(--color-text-light);
    font-size: 0.9rem;
    margin: 0;
  }

  /* Catering CTA */
  .catering-cta {
    background: var(--color-primary);
    padding: var(--spacing-md);
    border-radius: var(--radius-lg);
  }

  .catering-cta .cta-content {
    display: flex;
    align-items: center;
    gap: var(--spacing-md);
  }

  .cta-content h2 {
    color: white;
    margin: 0 0 0.25rem;
    font-size: 1.5rem;
  }

  .cta-content p {
    color: rgba(255, 255, 255, 0.9);
    margin: 0;
  }

  .catering-cta .btn {
    flex-shrink: 0;
    margin-left: auto;
  }

  .catering-cta .btn-primary {
    background: white;
    color: var(--color-primary);
    border-color: white;
  }

  .catering-cta .btn-primary:hover {
    background: var(--color-tertiary);
    border-color: var(--color-tertiary);
    color: var(--color-brown-dark);
  }

  @media (max-width: 768px) {
    .page-banner {
      height: 250px;
    }

    .order-page-wrapper {
      padding-top: 250px;
    }

    .main-order-btn {
      padding: 1.2rem 2.5rem;
      font-size: 1.4rem;
    }

    .pickup-content {
      grid-template-columns: 1fr;
    }

    .catering-cta .cta-content {
      flex-direction: column;
      text-align: center;
    }

    .catering-cta .btn {
      margin-left: 0;
    }
  }
</style>
