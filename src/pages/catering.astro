---
import Layout from '../layouts/Layout.astro';
---

<Layout
  title="Catering"
  description="Bring the fiesta to your event! Su Casa Mexican Restaurant offers catering services for parties, corporate events, weddings, and more in the Midvale, UT area."
>
<div class="catering-page-wrapper">
  <!-- Banner -->
  <section class="page-banner">
    <img src="https://i.postimg.cc/cJ7LtpDq/Copy-of-queso-dip-pic-(1).jpg" alt="Su Casa Catering" class="banner-image" />
  </section>

  <div class="catering-content">
    <div class="container">
      <!-- Catering Section -->
      <section class="catering-options">
        <h2 class="section-title">Catering</h2>
      </section>

      <!-- Inquiry Form -->
      <section class="inquiry-section" id="inquiry-form">
        <div class="form-container">
          <div class="form-header">
            <p>Please fill out the form below and we'll do our best to accommodate your desired date and any requests you may have to make your party or celebration memorable.</p>
          </div>
          <form class="catering-form" action="#" method="POST">
            <div class="form-row">
              <div class="form-group">
                <label for="name" class="form-label">Your Name *</label>
                <input type="text" id="name" name="name" class="form-input" required>
              </div>
              <div class="form-group">
                <label for="email" class="form-label">Email Address *</label>
                <input type="email" id="email" name="email" class="form-input" required>
              </div>
            </div>
            <div class="form-row">
              <div class="form-group">
                <label for="phone" class="form-label">Phone Number *</label>
                <input type="tel" id="phone" name="phone" class="form-input" required>
              </div>
              <div class="form-group">
                <label for="date" class="form-label">Event Date *</label>
                <input type="date" id="date" name="date" class="form-input" required>
              </div>
            </div>
            <div class="form-row">
              <div class="form-group">
                <label for="time" class="form-label">Event Time *</label>
                <input type="time" id="time" name="time" class="form-input" required>
              </div>
              <div class="form-group">
                <label for="guests" class="form-label">Number of Guests *</label>
                <input type="number" id="guests" name="guests" class="form-input" min="1" required>
              </div>
            </div>
            <div class="form-row">
              <div class="form-group">
                <label for="event-type" class="form-label">Event Type *</label>
                <select id="event-type" name="event-type" class="form-select" required>
                  <option value="">Select event type</option>
                  <option value="birthday">Birthday Party</option>
                  <option value="corporate">Corporate Event</option>
                  <option value="wedding">Wedding/Engagement</option>
                  <option value="holiday">Holiday Party</option>
                  <option value="graduation">Graduation</option>
                  <option value="other">Other</option>
                </select>
              </div>
              <div class="form-group">
                <label for="budget" class="form-label">Budget per Person</label>
                <div class="input-with-prefix">
                  <span class="input-prefix">$</span>
                  <input type="number" id="budget" name="budget" class="form-input form-input-prefixed" min="0" step="0.01" placeholder="0.00">
                </div>
              </div>
            </div>
            <div class="form-group">
              <label for="referral" class="form-label">How did you hear about us?</label>
              <input type="text" id="referral" name="referral" class="form-input" placeholder="e.g., Google, friend, social media...">
            </div>
            <div class="form-group">
              <label for="message" class="form-label">Additional Details</label>
              <textarea id="message" name="message" class="form-textarea" placeholder="Tell us about your event, dietary requirements, or any special requests..."></textarea>
            </div>
            <div class="form-submit">
              <button type="submit" class="btn btn-primary">Submit Inquiry</button>
              <p class="form-note">* Required fields. We'll respond within 24 hours.</p>
            </div>
          </form>
        </div>
      </section>

      <!-- Contact CTA -->
      <section class="contact-cta">
        <div class="cta-content">
          <h2>Prefer to Talk?</h2>
          <p>Give us a call to discuss your catering needs directly with our team</p>
          <a href="tel:+18012551042" class="btn btn-secondary">Call (801) 255-1042</a>
        </div>
      </section>
    </div>
  </div>
</div>
</Layout>

<style>
  /* Banner */
  .page-banner {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 350px;
    overflow: hidden;
    z-index: -1;
  }

  .catering-page-wrapper {
    padding-top: 350px;
  }

  .banner-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .catering-content {
    padding: var(--spacing-xl) 0;
  }

  /* Catering Options */
  .catering-options {
    margin-bottom: var(--spacing-lg);
    text-align: center;
  }

  .catering-options .section-title {
    display: inline-block;
    background: linear-gradient(135deg, var(--color-primary) 0%, var(--color-tertiary) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    font-size: 2.5rem;
    margin-bottom: var(--spacing-xs);
  }

  .catering-options::after {
    content: '';
    display: block;
    width: 80px;
    height: 4px;
    background: linear-gradient(90deg, var(--color-primary), var(--color-tertiary));
    margin: var(--spacing-sm) auto 0;
    border-radius: 2px;
  }

  .options-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: var(--spacing-md);
    margin-top: var(--spacing-lg);
  }

  .option-card {
    background: white;
    padding: var(--spacing-md);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
    transition: all var(--transition-normal);
    border-top: 4px solid var(--color-tertiary);
  }

  .option-card:hover {
    transform: translateY(-5px);
    box-shadow: var(--shadow-lg);
  }

  .option-card h3 {
    color: var(--color-brown-dark);
    margin: 0 0 var(--spacing-xs);
  }

  .option-desc {
    color: var(--color-text);
    font-size: 0.95rem;
    margin-bottom: var(--spacing-sm);
  }

  .option-includes {
    list-style: none;
    padding: 0;
    margin: 0;
    background: var(--color-cream);
    padding: var(--spacing-sm);
    border-radius: var(--radius-md);
  }

  .option-includes li {
    padding: 0.35rem 0;
    padding-left: 1.5rem;
    position: relative;
    font-size: 0.9rem;
    color: var(--color-text);
  }

  .option-includes li::before {
    content: '\2713';
    position: absolute;
    left: 0;
    color: var(--color-green);
    font-weight: bold;
  }

  /* Inquiry Form */
  .inquiry-section {
    background: linear-gradient(135deg, #fff 0%, #fef9f3 100%);
    padding: var(--spacing-lg);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-md);
    margin-bottom: var(--spacing-lg);
    border-top: 5px solid var(--color-primary);
    border-bottom: 3px solid var(--color-tertiary);
  }

  .form-header {
    text-align: center;
    margin-bottom: var(--spacing-md);
    padding: var(--spacing-sm);
    background: linear-gradient(90deg, var(--color-primary), var(--color-tertiary));
    -webkit-background-clip: text;
    background-clip: text;
  }

  .form-header p {
    color: var(--color-text);
    margin: 0;
    font-size: 1.05rem;
  }

  .form-label {
    color: var(--color-brown-dark);
    font-weight: 600;
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: var(--spacing-sm);
  }

  .form-submit {
    text-align: center;
    margin-top: var(--spacing-md);
  }

  .form-note {
    color: var(--color-text-light);
    font-size: 0.85rem;
    margin-top: var(--spacing-sm);
  }

  /* Input with prefix */
  .input-with-prefix {
    display: flex;
    align-items: center;
    position: relative;
  }

  .input-prefix {
    position: absolute;
    left: 12px;
    color: var(--color-text);
    font-weight: 500;
    z-index: 1;
  }

  .form-input-prefixed {
    padding-left: 28px;
  }

  /* Contact CTA */
  .contact-cta {
    text-align: center;
    padding: var(--spacing-lg);
    background: linear-gradient(135deg, var(--color-tertiary) 0%, #f5a623 100%);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-md);
  }

  .contact-cta h2 {
    color: white;
    margin-bottom: var(--spacing-xs);
    text-shadow: 1px 1px 2px rgba(0,0,0,0.2);
  }

  .contact-cta p {
    color: white;
    margin-bottom: var(--spacing-sm);
  }

  .contact-cta .btn-secondary {
    background: white;
    color: var(--color-brown-dark);
    border: none;
    font-weight: 600;
  }

  .contact-cta .btn-secondary:hover {
    background: var(--color-cream);
    transform: translateY(-2px);
  }

  @media (max-width: 768px) {
    .page-banner {
      height: 250px;
    }

    .catering-page-wrapper {
      padding-top: 250px;
    }

    .form-row {
      grid-template-columns: 1fr;
    }
  }
</style>
